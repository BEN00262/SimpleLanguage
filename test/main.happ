fun fizzbuzz(number) {
    if number%3 == 0 {
        if number%5 == 0 {
            return "fizzbuzz"
        }
        return "buzz"
    } else if number%5 == 0 {
        return "fizz"
    }
}

# be able to write a server with this language :) ( support a bunch of other stuffs )
fun router(path, handler) {
    # this is a simple router
}

# all of these require the maps data structure to work efficiently :(
router(
    "/",
    fun (request, response) {

    }
)

for (def counter = 0; counter < 20; counter + 1) {
    if counter%3 == 0 {
        if counter%5 == 0 {
            print("fizzbuzz ====> {counter}")
        }
        print("buzz ====> {counter}")
    } else if counter%5 == 0 {
        print("fizz ====> {counter}")
    }  
}