```bash:
    # generating the documentation from the file
    sampleLang -filename code.txt -mode l -operation d -out sample.html

    #executing the code in the file
    simpleLang -filename code.txt -mode l -operation e
```