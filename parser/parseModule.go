package parser

// each export should be a namespace
// not included stuffs ---> find a way to do lazy includes

func (parser *Parser) ParseModule() interface{} {
	// should return an interface
	return nil
}
